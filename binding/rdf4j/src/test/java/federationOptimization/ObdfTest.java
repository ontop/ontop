package federationOptimization;

/**
 * JUnit category for enabling optional OBDF-related units tests.
 * <p>
 * To mark a test as an OBDF one, annotate its class with {@code @Category(federationOptimization.ObdfTest.class}. Then enable Maven profile
 * {@code obdf} (in IntelliJ, right panel, under profiles click on obdf).
 * </p>
 */
public interface ObdfTest {
}
