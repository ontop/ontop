version: '3.4'
services:

  source-s1:
    image: postgres:14-alpine
    container_name: 'postgres_10009'
    restart: unless-stopped
    ports:
      - '10009:5432'
#    volumes:
#      - ./initialise-postgres:/docker-entrypoint-initdb.d
#      - ./data/${SCALE}:/data
#      - data-source-s1:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: 'smatv'
      POSTGRES_USER: 'obdf'
      POSTGRES_PASSWORD: 'obdfPwd0'
    shm_size: 1g