package it.unibz.inf.ontop.executor.pullout;

import it.unibz.inf.ontop.executor.SimpleNodeCentricInternalExecutor;
import it.unibz.inf.ontop.pivotalrepr.DataNode;
import it.unibz.inf.ontop.pivotalrepr.proposal.PullVariableOutOfDataNodeProposal;

/**
 * TODO: explain
 */
public interface PullVariableOutOfDataNodeExecutor
        extends SimpleNodeCentricInternalExecutor<DataNode, PullVariableOutOfDataNodeProposal> {
}
