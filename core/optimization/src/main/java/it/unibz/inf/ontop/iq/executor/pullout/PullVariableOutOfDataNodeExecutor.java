package it.unibz.inf.ontop.iq.executor.pullout;

import it.unibz.inf.ontop.iq.executor.SimpleNodeCentricExecutor;
import it.unibz.inf.ontop.iq.node.DataNode;
import it.unibz.inf.ontop.iq.proposal.PullVariableOutOfDataNodeProposal;

/**
 * TODO: explain
 */
public interface PullVariableOutOfDataNodeExecutor
        extends SimpleNodeCentricExecutor<DataNode, PullVariableOutOfDataNodeProposal> {
}
