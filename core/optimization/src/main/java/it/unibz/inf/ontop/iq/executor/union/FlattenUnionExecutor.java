package it.unibz.inf.ontop.iq.executor.union;

import it.unibz.inf.ontop.iq.executor.SimpleNodeCentricExecutor;
import it.unibz.inf.ontop.iq.node.UnionNode;
import it.unibz.inf.ontop.iq.proposal.FlattenUnionProposal;

public interface FlattenUnionExecutor extends SimpleNodeCentricExecutor<UnionNode, FlattenUnionProposal>{
}
